"""Optional Rust trusted-kernel extension for V-Loop.

The main :mod:`vloop` package deliberately treats this distribution as an
optional accelerator.  Consumers should use ``vloop.native_backend`` rather
than importing this module directly.
"""
