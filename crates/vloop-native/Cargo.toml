[package]
name = "vloop-native"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
name = "_core"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.26", features = ["abi3-py311", "extension-module"] }
vloop-core = { path = "../vloop-core" }
